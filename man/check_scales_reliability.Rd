% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelling_tools.R
\name{check_scales_reliability}
\alias{check_scales_reliability}
\title{Check the internal reliability of the VVIQ, TAS-20 and its sub-scales}
\usage{
check_scales_reliability(
  data,
  ...,
  scales = c("vviq", "tas", "dif", "ddf", "eot"),
  digits = 2,
  silence = FALSE
)
}
\arguments{
\item{data}{A data frame with one row per group and a list-column named
\code{items}, where each element is a data frame of item-level responses
(columns \code{vviq_q1}...\code{vviq_q16}, \code{tas_q1}...\code{tas_q20}).}

\item{...}{Grouping variables (e.g. \code{study}), used to compute reliability
separately within each group.}

\item{scales}{String vector with the names of the scales to examine. Has to
be one or several among the defaults: "vviq", "tas", "dif", "ddf" and "eot".}

\item{digits}{Number of decimal places to round the reliability
coefficients to. Default is 2.}

\item{silence}{Logical. If \code{TRUE}, suppresses the messages and warnings
commonly emitted by \code{psych::alpha()} and \code{psych::omega()} (e.g. about
reversed items or factor structure). Default is \code{FALSE}.}
}
\value{
A tibble with one row per group per scale, and columns
\code{Scale}, \verb{Cronbach's alpha}, \verb{McDonald's omega}, plus the grouping
variables passed via \code{...}.
}
\description{
Computes Cronbach's alpha and McDonald's omega for the VVIQ and the
TAS-20 (total score and its three subscales - DIF, DDF, EOT), separately
for each group defined by \code{...} (e.g. per study). Expects \code{data} to
contain a list-column named \code{items}, itself holding, for each group, a
data frame of item-level responses with columns named \verb{vviq_q*} and
\verb{tas_q*}.
}
\examples{
\dontrun{
check_scales_reliability(all_data, study)
check_scales_reliability(all_data, study, digits = 3, silence = TRUE)
}
}
