% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_vviq_marginal_histogram.R
\name{plot_vviq_marginal_histogram}
\alias{plot_vviq_marginal_histogram}
\title{Plot the marginal VVIQ distribution (floor spike + continuous remainder)}
\usage{
plot_vviq_marginal_histogram(
  data,
  binwidth = 1,
  y_lab = "Participant count",
  vviq_breaks = seq(16, 80, 4),
  base_theme = ggplot2::theme_minimal,
  ...
)
}
\arguments{
\item{data}{The data frame containing \code{vviq} and \code{complete_aphant}
(must match the same definition used to fit floor_group_additive).}

\item{binwidth}{Histogram bin width, in VVIQ-scale units. Default 2.}

\item{vviq_breaks}{x-axis breaks — MUST MATCH plot_floor_group()'s
\code{vviq_breaks} argument if composing the two via patchwork, or the panels
will misalign. Default \code{seq(16, 80, 4)}, matching plot_floor_group()'s
own default.}

\item{base_theme}{A ggplot2 theme function. Default \code{ggplot2::theme_minimal}.}

\item{...}{Additional arguments passed to \code{theme_pdf()}.}
}
\value{
A ggplot2 object (a plain histogram, NOT a patchwork composite —
compose with plot_floor_group() yourself, e.g. via \code{/} from patchwork).
}
\description{
Shows the empirical VVIQ distribution across the whole sample as a
histogram, with the floor bin (VVIQ=16) coloured to match
\code{plot_floor_group()}'s violin, and the remainder coloured by a viridis
gradient matching that function's scatter — visually establishing the
"floor spike + continuum" pattern that motivates the floor-group model.
}
