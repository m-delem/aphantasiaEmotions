% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelling_tools.R
\name{check_knot_still_matches}
\alias{check_knot_still_matches}
\title{Check that a segmented model's cached knot still matches a fresh MARS estimate}
\usage{
check_knot_still_matches(data, seed_knot, formula = tas ~ vviq)
}
\arguments{
\item{data}{The data the live model-comparison page is using right now
(e.g. \code{all_data}).}

\item{seed_knot}{The knot value that was used to seed the cached model's
prior when it was originally fit - a fixed constant recorded at fit time,
not read from the model object itself.}

\item{formula}{Formula passed to \code{earth::earth()}. Default is
\code{tas ~ vviq}, matching the segmented model's own formula.}
}
\value{
Invisibly, the freshly-computed knot value, if it matches
\code{seed_knot}. Errors loudly otherwise.
}
\description{
The segmented \code{brms} model's breakpoint prior is seeded from a fast,
frequentist MARS knot search (\code{earth::earth()}), then the model is fit once
and cached to disk with \code{file_refit = "never"} (see
\code{\link[=fit_brms_model]{fit_brms_model()}} and the "Continuous alternatives" section of
\code{vignette("model-comparison", package = "aphantasiaEmotions")}). Because the
cached model is never automatically refitted, it can silently go stale if
the underlying data changes - the site would keep reporting a knot fit
around old data without any visible sign that anything is wrong.
}
\details{
This function is a safeguard against exactly that. It re-runs the cheap
\code{earth::earth()} search on the data supplied \emph{now}, and compares the knot
it finds against \code{seed_knot} - the knot value that was actually used to
seed the prior for the cached model at the time it was fit (\strong{not} the
brms model's own posterior estimate of the knot, which is expected to
differ from the earth seed even on unchanged data, since brms re-estimates
it from a prior rather than reproducing earth's point estimate exactly).
If the two don't match exactly, the underlying data has almost certainly
changed since the model was cached, and the model should be refit.
}
